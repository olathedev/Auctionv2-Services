export enum NODE_ENV {
    developement='development',
    prodcution='production'
}

export enum propertyStatus {
    AVAILABLE="available",
    PENDING="pending",
    TAKEN="taken"
}

export enum Role {
    TENANT="tenant",
    LANDLORD="landlord",
    ADMIN="admin"
}